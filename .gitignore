# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
wheels/
*.egg

# Virtual environments
.venv/
venv/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Environment
.env

# Data (large files, not for git) — root-level only
/data/
*.duckdb
*.duckdb.wal
*.parquet

# Models — all committed (total 12MB, well within GitHub limits)
# Largest individual file: ~1.7MB (xgboost_model.joblib)
# All final models + per-fold checkpoints + OOF predictions tracked in git
# so trained models can be loaded from any machine after git pull

# CatBoost training artifacts
catboost_info/

# OS
.DS_Store
Thumbs.db
nul

# Jupyter
.ipynb_checkpoints/

# Testing
.coverage
htmlcov/
.pytest_cache/

# Claude
.claude/

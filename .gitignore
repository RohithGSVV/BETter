# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
wheels/
*.egg

# Virtual environments
.venv/
venv/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Environment
.env

# Data (large files, not for git) — root-level only
/data/
*.duckdb
*.duckdb.wal
*.parquet

# Models — GBM binaries are large (50-150MB), keep them local only
/models/gbm_ensemble/
/models/xgboost/
/models/lightgbm/
/models/catboost/
# Per-fold checkpoints for all models (reproducible, no need to commit)
/models/*/fold_*/
# OOF predictions file (large numpy arrays, regenerated by training)
/models/oof_predictions.joblib
# Lightweight final models ARE tracked in git:
#   models/bayesian_kalman/final/ (~50KB)
#   models/monte_carlo/final/     (~50KB)
#   models/meta_learner/final/    (~100KB)

# CatBoost training artifacts
catboost_info/

# OS
.DS_Store
Thumbs.db
nul

# Jupyter
.ipynb_checkpoints/

# Testing
.coverage
htmlcov/
.pytest_cache/

# Claude
.claude/
